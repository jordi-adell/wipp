language: cpp

addons:
 apt:
  sources:
   - george-edison55-precise-backports # cmake 3.2.3
  packages:
   - cmake
   - cmake-data
   - libfftw3-dev   

branches:
 only:
  - master

before_script:
 - mkdir build
 - cd build
 - cmake -Dtest=yes ../

script:
 - make test

